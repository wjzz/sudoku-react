import React from "react";
import "../App.css";

const Header : React.FC = () => {
    return (
        <header>
            Example Sudoku Solver in React
        </header>
    );
}

export default Header;